// Historical founder-create browser journey retired by Stage 1 Product Contract V2.
// Canonical seller → pending → founder moderation → public listing acceptance now lives in
// scripts/stage1-self-service-browser-e2e.ts and .github/workflows/stage1-self-service-acceptance.yml.
//
// Keep this file as an explicit compatibility marker because older workflow history names it.
// Founder-as-data-entry must not be reintroduced merely to satisfy the historical test harness.
console.log(
  "Legacy founder-create E2E is superseded by the Stage-1 self-service browser acceptance.",
);
