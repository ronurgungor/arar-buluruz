# V0 KVKK-min and publish evidence

_Date: 2026-07-31, Europe/Istanbul_

## Implemented scope

- Added `/gizlilik` describing only the current synthetic/mock V0 behavior.
- Added a visible, simple home-page privacy link.
- Removed `fonts.googleapis.com` and `fonts.gstatic.com` requests.
- Replaced the external font with a system UI stack.
- Extended V0 browser checks for the privacy route, no application cookie and no Google Fonts, analytics, advertising-network, auth or backend request.

No backend, Supabase activation, auth, real data, paid service, new dependency, cookie banner, advertising, analytics, TWA or Play Store scope was added.

## Pre-publish repository evidence

- Starting `main`: `e1bc613c4f5275a2a96d18a0d96c0434d80f38de`.
- PR: #30.
- Approved head: `71b026e0162be224655bd6477a4d44a487bcfe5c`.
- Normal merge commit: `399489b3a452a22664136bc43115cc796cf71fc6`.
- Changed files: eight; no `package.json` or `bun.lock` change.
- Standard CI run `30635858048`: success.
- V0 PWA run `30635858083`: success.
- V0 screenshot artifact: `8795292588`.
- Artifact SHA-256: `c6f8914c0fe51523c0076a5896988ec69bd58f46f62fe1cfa21e5347aa79deb1`.

Validated before publish:

- frozen Bun install and Bun-only boundary;
- lint and 7/7 unit tests;
- synthetic V0 build;
- disposable Gate 1 migration/RLS/REST regression with 22/22 pgTAP;
- desktop/mobile browser regression;
- privacy route and required disclosure;
- manifest, icons and controlling service worker;
- shell-only cache and honest offline fallback;
- no application cookie, Google Fonts, analytics/ad-network, auth or backend request.

## Publish attempt

- Lovable deployment ID: `36edff5e-7334-4d6f-b6da-60d0e35c69dd`.
- Published source: `399489b3a452a22664136bc43115cc796cf71fc6`.
- Public URL: `https://arar-buluruz.lovable.app`.

Public verification confirmed the V0 notice and application shell. It then found a release-blocking failure:

- `/ara?q=traktör&il=Tüm+Türkiye&sirala=yakin` returned HTTP 200;
- the page showed “İlanlar henüz gösterilemiyor” and “Pilot ilan bağlantısı henüz etkin değil”;
- synthetic listings were not available on the public runtime.

The deployment therefore failed the required synthetic-listing acceptance condition. Temporary public-verification PR #31 was closed without merge.

## Rollback limitation

The available Lovable connector supports publishing the current project but does not expose historical deployment selection, rollback or unpublish. Programmatic rollback could not be completed.

Required founder-side UI action:

1. History → previous accepted published revision → Revert → Publish/Update; or
2. Project Settings → Unpublish.

No listing-source/environment repair or republish is authorized before this rollback/unpublish action.

## Unexpected Lovable mutation and cost

A Lovable agent was explicitly instructed not to edit code/settings and to perform only deployment rollback if supported. It stated that it made no mutation, but it:

- consumed `1.3` Lovable credits;
- pushed a commit changing `package.json`, `bun.lock` and `src/routeTree.gen.ts`;
- pushed an empty follow-up commit.

This violated the zero-cost and no-rebuild boundaries.

Repository recovery:

- PR: #33.
- Recovery head: `d01943f2fe29a859a49e6a53a9d2532804da9b6c`.
- Standard CI run `30638080247`: success.
- V0 PWA run `30638082278`: success.
- Normal recovery merge: `edabc518643bc9ae102df1149817ecb3d96f003c`.
- Comparing accepted merge `399489b3…` to recovery merge `edabc518…` shows no file difference.
- No force-push or history rewrite was used.

## Final status

- GitHub canonical content is recovered and fully validated.
- Public deployment remains failed/unaccepted until the founder completes History/Revert + Publish/Update or Unpublish in the Lovable UI.
- Backend, real data, auth, storage, advertising, analytics, TWA, Play Store and paid-service gates remain closed.
