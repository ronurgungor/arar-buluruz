# V0 KVKK-min pre-publish evidence

_Date: 2026-07-31, Europe/Istanbul_

## Scope

- Add a minimum `/gizlilik` route describing only the current synthetic/mock V0 behavior.
- Add a visible, simple home-page privacy link.
- Remove `fonts.googleapis.com` and `fonts.gstatic.com` requests.
- Use the existing system UI font stack only.
- Extend V0 browser checks for the privacy route, zero cookies and no Google Fonts, analytics, advertising, auth or backend requests.

## Explicit exclusions

No backend, Supabase activation, auth, real data, paid service, dependency, cookie banner, advertising, analytics, TWA, Play Store or other product scope is added.

## Repository evidence

- Base `main`: `e1bc613c4f5275a2a96d18a0d96c0434d80f38de`.
- PR: `#30` — `Add V0 privacy page and remove Google Fonts`.
- Approved PR head: `71b026e0162be224655bd6477a4d44a487bcfe5c`.
- Normal merge commit and current published source SHA: `399489b3a452a22664136bc43115cc796cf71fc6`.
- Changed files: eight; no package, lockfile, dependency, backend, environment or paid-service change.

## CI evidence

Standard CI run `30635858048` passed:

- frozen Bun install;
- Bun-only package boundary;
- lint;
- unit tests;
- synthetic/backend-disabled build;
- isolated Gate 1 migration, 22/22 pgTAP RLS/database tests, REST integration and desktop/mobile regression.

V0 PWA run `30635858083` passed:

- manifest, icons, service-worker and honest offline fallback;
- desktop/mobile V0 flows;
- `/gizlilik` route and disclosure text;
- zero application cookies;
- no Google Fonts, analytics, advertising, auth or backend request;
- no real listing, account or complaint-data collection.

## Publish evidence

- Lovable project: `dca896f8-bb48-4a67-ae49-0493610ca6ad`.
- Public URL: `https://arar-buluruz.lovable.app`.
- Deployment ID: `0012cfb1-031b-41dc-a15f-9f1697268d9c`.
- Publish request was accepted on 2026-07-31.
- Lovable project metadata subsequently reported the project as published and updated, with the latest screenshot/runtime source identifier containing `399489b3`, matching current GitHub `main`.

## Public verification result and limitation

The deployed source identity matches current `main`, and all requested runtime behaviors passed the same-SHA automated browser suite before merge. Lovable metadata confirms the publish update.

A second independent direct request to the public hostname could not be completed from the execution container because its DNS resolver temporarily failed for `lovable.app`. This is an external verification-environment limitation, not an observed application failure. No rollback was applied because no published-runtime defect was detected and Lovable's own published-project metadata showed the expected current source identity.

## Rollback

No rollback was required. If a real public-runtime defect is later observed, restore and publish the last accepted Lovable revision and revert merge commit `399489b3a452a22664136bc43115cc796cf71fc6` without expanding scope.
